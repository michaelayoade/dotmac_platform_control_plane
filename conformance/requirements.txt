# Runtime dependencies the conformance suite itself needs, on top of the two
# real wheels (dotmac_deployment_control, dotmac_deployment_foundation)
# installed separately per conformance/README.md. Pinned to the exact
# versions this repository's own poetry.lock already resolves for
# dotmac-kernel's transitive dependencies, so the disposable conformance venv
# never disagrees with what this repository's own dependency graph proved
# works together (checked via `poetry.lock` at scaffold time, 2026-09-22):
#
#   sqlalchemy   2.0.51
#   cryptography 50.0.0
#   pytest       8.4.2
#   psycopg      3.3.4
#   alembic      1.18.5
#
# Re-check these against this repository's own poetry.lock before building
# the disposable venv, in case this branch has since taken a dependency
# bump -- do not assume these pins are still current without re-deriving
# them.

sqlalchemy==2.0.51
cryptography==50.0.0
pytest==8.4.2
psycopg[binary]==3.3.4
alembic==1.18.5
